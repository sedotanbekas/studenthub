import type { AnyContract } from "@/lib/http/contract";

/** Kontrak route domain attendance (diisi oleh fase implementasi domain ini). */
export const attendanceContracts: readonly AnyContract[] = [];
