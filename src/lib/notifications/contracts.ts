import type { AnyContract } from "@/lib/http/contract";

/** Kontrak route domain notifications (diisi oleh fase implementasi domain ini). */
export const notificationsContracts: readonly AnyContract[] = [];
