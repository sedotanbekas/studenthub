import type { AnyContract } from "@/lib/http/contract";

/** Kontrak route domain dashboard (diisi oleh fase implementasi domain ini). */
export const dashboardContracts: readonly AnyContract[] = [];
