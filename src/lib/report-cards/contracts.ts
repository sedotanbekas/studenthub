import type { AnyContract } from "@/lib/http/contract";

/** Kontrak route domain report-cards (diisi oleh fase implementasi domain ini). */
export const reportCardsContracts: readonly AnyContract[] = [];
