import type { PolicyRule } from "./types";

/** Aksi POLICY domain schools (diisi oleh fase implementasi domain ini). */
export const schoolsPolicy = {} as const satisfies Record<string, PolicyRule>;
