import type { PolicyRule } from "./types";

/** Aksi POLICY domain calendar (diisi oleh fase implementasi domain ini). */
export const calendarPolicy = {} as const satisfies Record<string, PolicyRule>;
