import type { PolicyRule } from "./types";

/** Aksi POLICY domain sponsors (diisi oleh fase implementasi domain ini). */
export const sponsorsPolicy = {} as const satisfies Record<string, PolicyRule>;
