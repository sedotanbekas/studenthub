import type { PolicyRule } from "./types";

/** Aksi POLICY domain students (diisi oleh fase implementasi domain ini). */
export const studentsPolicy = {} as const satisfies Record<string, PolicyRule>;
