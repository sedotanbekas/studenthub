import type { AnyContract } from "@/lib/http/contract";

/** Kontrak route domain announcements (diisi oleh fase implementasi domain ini). */
export const announcementsContracts: readonly AnyContract[] = [];
